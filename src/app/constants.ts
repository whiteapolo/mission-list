import { MissionStatus, MissionStatusFilter } from './types';

export const MISSIONS_LOCAL_STORAGE_KEY = 'missions';

export const MISSION_STATUS_TYPES = Object.values(MissionStatus);

export const MISSION_STATUS_FILTERS = Object.values(MissionStatusFilter);
